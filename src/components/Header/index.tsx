import { HeaderContainer, HeaderWrapper, HeaderTitle } from "./Header.styles";

function Header() {
  return (
    <HeaderContainer>
      <HeaderWrapper>
        <HeaderTitle>Hacker News</HeaderTitle>
      </HeaderWrapper>
    </HeaderContainer>
  );
}

export default Header;
